#!/usr/bin/env python3
# Student ID: mzaman30

import subprocess
from datetime import datetime, timedelta

def get_inactive_users(days=30):
    """Return list of users who have not logged in for X days."""
    # (same function as before, with comments)
    ...

def get_frequent_users(threshold=5):
    """Return dict of users who logged in >= threshold times."""
    # (same function as before, with comments)
    ...
